@page
@model ProjectStation.Pages.Exhibition.TigSummer2020Model
@{
    ViewData["Title"] = "tigSummer2020";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}


    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="fullMediumHeader">I_AM_BECOME.pdf Michael Ho</div>
                <div class="fadedLine"></div>
                <div id="vrview"></div>
            </div>
        </div>

        @for (int i = 0; i < Model.ArtPieces.Count; i++)
        {
            <div class="row">
                <div class="col-md-12">
                    <a asp-page="/Exhibition/Works" asp-route-id=@Model.ArtPieces[i].Id><img src="@Url.Content(Model.ArtPieces[i].PhotoPath)"/></a>
                </div>
            </div>
        }

        @*@foreach (var artPiece in Model.ArtPieces)
        {

        }*@
    </div>

    

